{
  "name": "universal-agent-bridge",
  "version": "1.0.2",
  "description": "Universal multi-CLI agent bridge for Claude Code",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "bridge": "node plugins/agent-bridge/dist/bridge.js",
    "dev": "npx tsx plugins/agent-bridge/scripts/bridge.ts",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
