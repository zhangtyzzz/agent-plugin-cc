{
  "name": "universal-agent-bridge",
  "version": "1.0.0",
  "description": "Universal multi-CLI agent bridge for Claude Code",
  "type": "module",
  "scripts": {
    "bridge": "npx tsx plugins/agent-bridge/scripts/bridge.ts",
    "test": "npx tsx plugins/agent-bridge/scripts/bridge.ts --task health"
  },
  "dependencies": {
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
